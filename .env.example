# Database password (Dashboard → Project Settings → Database). Used only for
# optional local admin scripts — NEVER put this in the browser app.
SUPABASE_PWORD=

# Project URL (Dashboard → Project Settings → API)
SUPABASE_URL=https://ayicngoasguoqegxoptd.supabase.co

# anon / public key (Dashboard → Project Settings → API). Safe to embed in the
# frontend; security is enforced by Row Level Security + Auth.
SUPABASE_ANON_KEY=
